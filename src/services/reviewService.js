const getReviews = async movieId => {
  console.log('Get Reviews', movieId)
}

const createReview = async reviewData => {
  console.log('Create Review', reviewData)
}

export default {
  getReviews,
  createReview,
}